# CLAUDE.md

이 파일은 이 저장소에서 코드를 작성하는 AI(Claude Code)를 위한 규칙서다.

## 이 프로젝트

**기업 환경 모니터링** — 재정경제부 기업환경과 실무자 5~10명이 쓰는 내부 웹 서비스.
국내 기업 규제·애로사항 기사를 매일 자동으로 수집·요약해 보여준다.

기획 원본은 [PRD.md](PRD.md)다. **PRD.md와 이 파일이 어긋나면 PRD.md가 맞다** —
이 파일은 PRD.md를 코드 작업에 맞게 요약한 것이지, 별도 결정을 담은 문서가 아니다.
초기 논의 과정은 [prd_lite.md](prd_lite.md)에 남아 있다.

## 기술 스택

- **Next.js** — 화면(App Router)과 서버 API를 한 프로젝트에서 처리. **PRD에 정한 대로
  고정이며, 다른 프레임워크로 바꾸거나 마이그레이션을 제안하지 않는다** (PRD 8번,
  과정 지침으로 고정됨).
- **Supabase** — 기사 저장용 테이블 `articles` 1개.
- **네이버 뉴스 검색 API** — 기사 수집.
- **OpenAI API** (`gpt-5-mini`) — 기사 요약. 원래 PRD는 Claude API였으나 Anthropic
  키가 없어 이미 있던 OPENAI_API_KEY로 바꿨다 (PRD 8번 참고). `gpt-5-mini`는 추론
  모델이라 `reasoning_effort: 'minimal'`을 반드시 지정한다 — 안 하면 토큰 예산을
  내부 추론에 다 쓰고 빈 응답을 낸다.
- **Vercel** — 배포와 Cron(매일 오전 8시 KST 자동 수집)에 사용한다.

## 절대 지켜야 할 규칙

숫자가 들어간 규칙은 PRD에서 이미 합의된 값이다. **임의로 다른 값을 쓰지 않는다.**
바꿔야 할 이유가 보이면 코드를 먼저 고치지 말고 사용자에게 확인한다.

### 수집 (PRD 5-1)

- 자동 수집은 하루 1회, 오전 8시(KST)에만.
- 검색어는 **직접 규제 4개**(`기업 규제`, `기업 인허가`, `기업 인증`, `기업 애로`) +
  **산업 8개**(`자동차 업계`, `철강 업계`, `조선 해운 업계`, `에너지 석유화학 업계`,
  `바이오 제약 업계`, `금융권`, `반도체 업계`, `정보통신 플랫폼 업계`), 총 12회로
  나눠 호출한다 (OR 검색 불가).
- 최초 1회는 3개월 소급 + 상위 20건만. 이후 매일은 최근 7일 이내 기사만.
- **연관성 점수(0~100)와 상한 상수는 `lib/relevance.ts` 한 곳에서만 관리한다.**
  숫자를 다른 파일에 다시 적지 않는다.
  - `MIN_RELEVANCE_SCORE = 60` — 이 점수 미만은 **요약하지도, 저장하지도, 목록에
    노출하지도 않는다.** 요약 API를 부르기 전에 걸러 비용을 쓰지 않는다.
  - `MAX_VISIBLE_ARTICLES = 30` — 한 번 수집할 때 저장·요약하는 대표 기사 상한이자
    화면 기본 목록 상한.
- **점수 계산에 별도 AI API를 부르지 않는다.** `calculateRelevanceScore(title,
  description, industries)` 순수 함수가 제목·발췌문(기존 기사는 제목+요약)과 산업
  분류 결과만으로 계산한다. 가점(제목 규제 신호 +25, 발췌문 규제 신호 +10, 기업
  부담 +25, 개선 요구 +20, 기업·산업 주체 +10, 산업 분류 +10)과 감점(정치 -30,
  주가·실적 -30, 행사·인사 -25, 사설·칼럼 -20, 기업 영향 없는 정책 소개 -20)을
  합산한 뒤 0~100으로 자른다.
- **필수 통과 조건**: ① 규제 신호·기업 부담·개선 요구 중 하나 이상, ② 기업·업계
  주체 또는 산업 분류 중 하나 이상. 정치·주가·행사·단순 정책 발표 기사는 점수와
  무관하게 제외하고, 산업 미분류 기사는 75점 이상 + 기업 부담·개선 요구가 있을
  때만 통과시킨다.
- 목록·수집 정렬은 **연관성 점수 내림차순, 동점이면 발행일 최신순**이다.
- 중복 판정: 제목 2-gram 자카드 유사도 **0.7 이상**이면 같은 사안. **지우지 않고
  대표 기사에 묶는다.**
- 이미 수집한 URL은 다시 담지 않는다.
- 수집 시점에 원문 링크 HEAD 요청으로 죽은 링크를 거른다.
- 90일 지난 기사는 삭제. 화면 기본 표시는 최근 7일.
- 수동 새로고침: **5분 쿨다운**, 횟수 제한은 없지만 하루 총합 30건(MAX_VISIBLE_ARTICLES) 상한은 그대로 적용된다.
  **쿨다운과 상한은 반드시 서버에서 강제한다** (화면 쪽 검증만으로는 우회 가능).
- **일회성 백필 수집 모드**(상한은 운영과 같은 `MAX_VISIBLE_ARTICLES`)는 운영용과 별개다. 쿨다운·
  `today_new_count`를 건드리지 않으며, API 라우트나 화면 버튼으로 실행할 수 없고
  `npm run backfill:collect`(`scripts/backfill-collect.ts`)로만 실행한다.

### 요약과 카드 (PRD 5-2)

- 3줄 이내, 한 줄 40자 이내 (공백 포함, 한글·영문·숫자 모두 1자).
- 카드 표시 항목은 **제목 / 언론사 / 발행일 / 요약 / 원문 링크 5가지 + (있을 때만)
  관련 기사 N건**. 그 외 정보를 추가하지 않는다.
- 원문 링크는 네이버 링크(`link`) 우선, 없으면 언론사 원문(`originallink`).
- "관련 기사 N건"은 카드 안에서 펼치고 접는다 — 별도 페이지로 이동하지 않는다.
  요약은 대표 기사 1건에만 만든다.
- 목록 정렬은 **연관성 점수 내림차순, 동점이면 발행일 최신순**이다 (산업 필터에서도 동일).
- 화면 상단에 마지막 수집 성공 시각을 항상 표시한다.
- 기사에 없는 내용을 추측해서 쓰지 않는다.
- 요약 실패(API 오류·타임아웃 / 재시도 1회 후에도 규칙 위반 / 빈 응답) 시 문장을
  지어내지 말고 "요약 실패" + 원문 링크만 남긴다.

## 보안

- API 키(네이버·Supabase·OpenAI)는 `.env`에만 두고 **서버 쪽 코드(API 라우트,
  Cron 라우트)에서만** 쓴다. 클라이언트 컴포넌트에서 절대 참조하지 않는다.
- `.env`와 `node_modules`는 `.gitignore`에 등록돼 있다. **절대 커밋하지 않는다.**
  둘 다 등록돼 있는지 커밋 전에 항상 확인한다.
- 새 비밀 값이 생기면 `.env.example`에 키 이름만 추가하고 값은 넣지 않는다.
- 배포는 URL 공개, 로그인 없음 — 의도된 설계다 (PRD 7번). 로그인 기능을 임의로
  추가하지 않는다.
- 기사 본문은 크롤링하지 않는다. 네이버 API의 발췌문(`description`)만 사용한다.

### 외부 서비스 인증

- 토큰 값을 사용자에게 묻거나 채팅에 출력하지 않는다. **항상 `.env`에 있는 값을
  코드/CLI가 직접 읽어서 쓴다.**
- Supabase 작업이 필요하면 Supabase CLI를 설치해 `.env`의 `SUPABASE_ACCESS_TOKEN`으로
  인증한다.
- Vercel 작업(배포 등)이 필요하면 Vercel CLI를 설치해 `.env`의 `VERCEL_TOKEN`으로
  인증한다.
- 값이 `.env`에 없으면 사용자에게 발급 절차를 안내하되, 값 자체를 입력하라고
  요구하지 않는다 (사용자가 직접 `.env`에 넣는다).

## 산업 필터 (PRD 5-3, 고정형)

- 화면 상단 드롭다운(전체 + 고정 8개 산업)으로 이미 수집된 기사를 거른다. **사용자
  태그가 아니라 시스템이 키워드로 분류하는 고정 필터**다 — 비범위인 "사용자 메모·
  코멘트·태그"와 혼동하지 않는다.
- 분류는 `lib/industries.ts`의 `classifyIndustries()` 하나로만 한다. 별도 AI API를
  부르지 않고, 제목+발췌문에서 고정 키워드를 찾을 뿐이다.
- 필터 전환은 클라이언트 상태로만 처리한다. 페이지 이동·URL 변경·서버 재호출이
  없어야 한다.
- 카드에 산업명·배지를 추가하지 않는다.

## 이번에 만들지 않는 것 (PRD 6번)

로그인/회원가입, 이메일·슬랙 알림, 통계·차트·대시보드, 메모·코멘트·태그, 규제 개선안
자동 생성, 해외 기사·번역, 네이티브 모바일 앱, 기사 본문 크롤링.

이 목록에 있는 걸 "더 좋아 보여서" 슬쩍 끼워 넣지 않는다. 필요하다고 판단되면 코드를
쓰기 전에 먼저 알린다.

## 디자인 (PRD prd_lite 5번)

- 네이비 `#1E3A5F` 기본, 강조색 파랑 `#2563EB` 하나만. 배경 흰색/연회색, 글자 진회색.
- 담백하고 신뢰감 있는 느낌 — 장식 최소화.
- **모바일 우선 반응형.** 모바일(767px 이하) 1열, 태블릿·데스크톱(768px 이상) 2열,
  전체 콘텐츠 최대 너비 1180px. 헤더·산업 필터는 전체 콘텐츠 너비를 쓰고, 기사
  목록만 grid로 나뉜다.
- 기사 카드 상단에 **산업별 대표 이미지**(장식용, 16:9)를 쓴다. `public/images/industries/`의
  **로컬 이미지만** 쓰고 기사 원문·언론사 사이트 이미지를 크롤링하지 않는다. 새 DB
  컬럼(`Article` 타입·Supabase 스키마)에 이미지 경로를 저장하지 않고, `lib/industries.ts`의
  `getArticleImage()`로 산업값에서 그때그때 계산한다. 산업 미분류나 파일 누락 시
  CSS 그라데이션으로 대체하고, 깨진 이미지 아이콘을 보여주지 않는다.
- 애니메이션은 Tailwind 유틸리티(`transition`, `motion-reduce:` 등)와 `globals.css`의
  keyframes만 쓴다. **Canvas·WebGL·외부 애니메이션 라이브러리를 추가하지 않는다.**
  `prefers-reduced-motion: reduce`에서는 애니메이션 없이도 모든 기능이 동일하게
  동작해야 한다.
- **컬러 모션 시스템**: 파랑 `#1A73E8`·인디고 `#4F46E5`가 기본, 시안 `#22D3EE`·
  바이올렛 `#7C3AED`가 보조, 그린·코랄은 포인트 전용이다(`globals.css`의 `--motion-*`).
  색은 그라데이션·미세 테두리·빛 확산·선택 상태에만 쓰고 본문 글자에는 쓰지 않는다.
  네온·사이버펑크·rainbow·무한 색상 순환은 쓰지 않는다.
- **반복 애니메이션은 인트로 안에서만** 제한적으로 허용한다(웨이브 3층, 8~14초).
  메인 화면 요소는 사용자 행동에 반응할 때만 1회 움직이고, 인트로가 끝나면 웨이브
  DOM과 타이머를 정리한다. 메인은 미세한 gradient·border·glow까지만 쓴다.
- **상단 영역**(페이지 배경 `#F8F9FA`, 흰색 둥근 헤더 패널, 둥근 산업 선택 영역,
  `#1A73E8` 새로고침 버튼)은 Google Workspace 계열의 담백한 톤으로 둔다.
- **기사 카드 디자인은 보호 대상이다.** 산업 이미지·제목 오버레이·본문 배치·1·2열
  grid·hover 이동·이미지 확대·관련 기사 펼치기 애니메이션·여백·글자 크기를 바꾸지
  않는다. 카드에 새로 추가된 것은 언론사 행 오른쪽의 **연관성 점수 배지 하나뿐**이며,
  점수 산정 근거·키워드 목록은 카드에 넣지 않는다.
- 서비스 표시명은 **기업 환경 모니터링**이다 (화면 헤더·metadata title·문서 공통).
  프로젝트 폴더명과 Supabase 테이블명은 바꾸지 않는다.
- **첫 진입 인트로**(`components/IntroOverlay.tsx`)는 별도 라우트가 아니라 메인 화면
  위에 덮는 overlay다. `sessionStorage`로 **브라우저 세션당 1회만** 띄우고, 인트로가
  떠 있는 동안에도 기사 데이터는 서버에서 이미 렌더링돼 뒤에 준비돼 있다 — 버튼을
  눌러도 API를 다시 부르지 않는다. 클릭 후 메인이 완전히 보일 때까지 900~1,200ms
  안에 끝내고, `prefers-reduced-motion`에서는 단순 fade로 대체한다. 업무 도구라
  인트로가 매번 접근을 방해해서는 안 된다.

## 작업 순서

PRD 9번 "개발 단위"에 21개 단위가 순서대로 정리돼 있다. 임의 순서로 건너뛰지 말고
그 순서를 따른다. 지금 목표 범위는 **1~5번(화면 + 샘플 데이터 + Supabase 연결)까지**다.

## 코드 작성 원칙

- 이 요구사항 목록에 없는 기능·설정·추상화를 미리 만들지 않는다.
- 위 "절대 지켜야 할 규칙"의 숫자·조건을 상수로 코드에 명확히 남긴다 (매직 넘버로
  흩어놓지 않는다).
- 주석은 왜(why)가 비직관적일 때만 남긴다. 무엇을 하는지는 코드 자체로 드러나게 짠다.
- **모든 설명과 주석은 한국어로 작성한다.** 변수·함수명 같은 식별자는 관례대로 영어를
  써도 되지만, 커밋 메시지·주석·문서·대화 응답은 한국어로 쓴다.
- **새 파일은 `my-app` 폴더 안에만 만든다.** 상위 폴더나 다른 위치에 파일을 만들지
  않는다.
- **코드를 바꾸면 무엇을·왜 바꿨는지 한 줄로 알린다.** 파일을 수정할 때마다 대화창에
  간단히 남긴다 (예: "새로고침 쿨다운을 3분에서 5분으로 수정 — PRD 재확인 결과 반영").
- **파일을 지워야 할 때는 바로 삭제하지 않는다.** `my-app/trash-can/` 폴더를 만들고
  그 안으로 옮겨만 둔다. 원래 폴더 구조를 알아볼 수 있게 하위 경로를 유지해서 옮긴다
  (예: `src/lib/old.ts` → `trash-can/src/lib/old.ts`). 최종 삭제는 사용자가 직접
  확인한 뒤 한다.
- **설치된 서브에이전트(bkit 등)를 필요할 때마다 적극 활용한다.** 작업 성격에 맞는
  에이전트가 있으면 직접 다 하려 하지 말고 위임한다.
