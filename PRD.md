# 기업 환경 모니터링 서비스 기획서 (PRD)

- 작성자: 영민
- 작성일: 2026-08-03

---

## 1. 추진 배경 (문제 정의)

*누가, 어떤 업무에서, 어떤 불편을 겪는지 적는 칸입니다.*

재정경제부 기업환경과 실무자는 기업이 겪는 규제 애로사항을 파악하고 개선·완화 방안을
검토하는 업무를 맡고 있다. 그 출발점이 되는 정보의 상당 부분은 언론 보도에서 나온다.

문제는 이 기사들이 수십 개 언론사에 흩어져 있다는 점이다. 실무자는 매일 아침 어떤 규제
이슈가 새로 제기됐는지 확인하기 위해 여러 매체를 일일이 찾아 읽어야 하고, 같은 사안을
다룬 기사를 중복해서 확인하는 데도 시간을 쓴다. 정작 필요한 것은 "어떤 기업이 무엇을
불편해하는가" 한 줄인데, 그것을 알아내려면 기사 전문을 읽어야 한다.

이 서비스는 국내 기업 규제 관련 애로사항·불편사항을 다룬 기사를 매일 자동으로 수집·
정리·요약해, 개선이 필요한 규제 이슈를 매일 빠르게 파악할 수 있게 해준다.

---

## 2. 현행 방식과 한계

*지금은 이 일을 어떻게 처리하고 있고, 무엇이 아쉬운지 적는 칸입니다.*

현재는 실무자가 매일 아침 포털 뉴스 검색과 주요 언론사 사이트를 직접 훑어보며 규제 관련
기사를 찾는다. 눈에 띄는 기사는 개인 메모나 문서에 따로 옮겨 적는다.

아쉬운 점은 다음과 같다.

- **누락이 생긴다.** 검색어를 무엇으로 넣느냐에 따라 결과가 달라져, 표현이 다른 기사는
  놓치기 쉽다.
- **같은 사안을 반복해서 읽는다.** 하나의 이슈를 여러 매체가 비슷하게 보도하는데, 매번
  새 기사인지 이미 본 기사인지 확인하는 데 시간이 든다.
- **요점을 뽑는 데 시간이 든다.** 애로사항 한두 줄을 파악하기 위해 기사 전문을 읽어야
  한다.
- **기록이 흩어진다.** 어제 무엇을 봤는지 개인 메모에만 남아 있어 이어보기가 어렵다.

---

## 3. 목표와 기대효과 (성공 기준)

*처리 시간, 문의 건수, 정답률처럼 측정 가능한 숫자로 적는 칸입니다.*

| 지표 | 현재 | 목표 |
|---|---|---|
| 아침 기사 확인 소요 시간 | 약 40분<sup>*</sup> | **10분 이내** |
| 하루 확인 기사 수 | 30~50건(중복 포함)<sup>*</sup> | **30건 이내** |
| 중복 기사 비중 | 30% 내외<sup>*</sup> | **5% 이하** |
| 요약 사실 오류 | — | 4주 누적 40건 검수 시 **0건** |
| 요약 형식 위반(3줄·40자 초과) | — | **5% 이하** |
| 규제 이슈 누락률(주간 수동 점검 대비) | — | **10% 이하** |

<sup>*</sup> **현재 값은 실측이 아닌 추정치다.** 운영 1주차에 실제로 측정해 이 표를 갱신한다.

**검수 방법**: 주 1회 무작위 10건을 뽑아 원문과 대조한다. 4주 누적 40건 기준으로
사실 오류와 형식 위반을 센다.

기대효과: 실무자가 기사를 "찾고 거르는" 일에서 벗어나, 확보한 시간을 규제 개선안 검토에
쓸 수 있게 된다.

---

## 4. 사용자와 이용 흐름

*누가 쓰는지와 사용 순서를 화살표로 적는 칸입니다.*

**사용자**: 재정경제부 기업환경과 실무자 **5~10명** (내부용, 계정 없이 바로 사용)
같은 화면을 여럿이 함께 본다. 로그인이 없어 개인별 구분은 하지 않으며, "누가 어디까지
읽었는지"는 다루지 않는다. **모바일 우선**으로 설계하되, 사무실 데스크톱·태블릿에서도
불편 없이 보도록 반응형으로 만든다 (아래 디자인 항목 참고).

**이용 흐름**

사이트 접속 → 화면 상단에서 마지막 수집 시각 확인 → 오늘 수집된 기사 목록 확인(발행일
최신순) → 카드의 3줄 요약으로 사안 파악 → 더 볼 기사는 원문 링크 열기
→ (필요 시) 새로고침으로 다시 수집 (마지막 수집 5분 후부터 가능)

**자동 수집 흐름**

매일 오전 8시(Vercel Cron) → 검색어 12개로 네이버 뉴스 검색 API 호출 → 기간 필터 →
URL 중복 제외 → 산업 분류 → 연관성 점수 계산 → 필수 통과 조건·60점 미만 제외 →
죽은 링크 확인 → 중복 묶기 → 연관성 점수순 상위 30건 선정 → 선정된 대표 기사만
요약 생성 → Supabase에 저장 → 목록에 반영

---

## 5. 주요 기능

*만들 기능을 must(반드시)와 nice(있으면 좋음)로 구분해 적는 칸입니다.*

### must

#### 1) 일일 규제 기사 자동 수집

- 설명: 매일 정해진 시각에 국내 언론 기사 중 기업 규제·애로사항 관련 기사를 자동으로
  수집해 목록으로 보여준다.
- AI가 꼭 지켜야 할 규칙:
  - **자동 수집은 하루 1회, 오전 8시(KST)에만 돈다.** 수동 새로고침은 횟수를 제한하지
    않되, **마지막 수집으로부터 5분이 지나야** 다시 누를 수 있다(쿨다운).
  - **검색어**: 네이버 검색 API는 OR 문법을 지원하지 않으므로 검색어를 각각 따로
    호출한다. **직접 규제 검색 4개**(`기업 규제`, `기업 인허가`, `기업 인증`, `기업 애로`)
    + **산업 검색 8개**(`자동차 업계`, `철강 업계`, `조선 해운 업계`, `에너지 석유화학
    업계`, `바이오 제약 업계`, `금융권`, `반도체 업계`, `정보통신 플랫폼 업계`), 총
    **검색어 12개로 나눠 호출**한다. 직접 규제 검색은 좁고 정확하게, 산업 검색은
    넓게 모은 뒤 아래 관련성 점수제로 거른다.
  - **수집 기간**: 최초 1회만 3개월치를 소급하되 **상위 20건만** 가져오고, 그 이후 매일은
    최근 7일 이내 발행 기사만 대상으로 한다.
  - **연관성 점수제 (0~100점)**: 기사마다 기업 규제·애로 연관성을 0~100점으로 매긴다.
    제목·발췌문(기존 기사는 제목+요약)과 산업 분류 결과만 쓰며, **점수 산정을 위한
    별도 AI 호출은 하지 않는다.** 가점·감점 항목은 아래와 같고, 각 항목은 여러 단어가
    걸려도 한 번만 적용한다.
    - 제목에 규제·제도 신호(`규제, 인허가, 허가, 승인, 인증, 심사, 신고, 등록,
      행정처분, 시행령, 시행규칙, 규제특례, 제도개선` 등) → **+25**
    - 발췌문에 규제·제도 신호 → **+10** (제목 가점과 별도)
    - 구체적인 기업 부담·사업 차질(`비용 부담, 투자 지연, 생산 차질, 진입장벽,
      승인 지연` 등) → **+25**
    - 기업·업계의 직접적인 개선 요구(`요구, 건의, 촉구, 호소, 반발, 유예를 요청` 등)
      → **+20**
    - 기업·산업 주체가 명확함(`기업, 중소기업, 업계, 협회, 금융권, 반도체업계` 등)
      → **+10**
    - 8개 산업 중 하나 이상으로 분류됨 → **+10**
    - 감점: 정치·선거 **-30**, 단순 주가·실적·시황 **-30**, 행사·인사·홍보 **-25**,
      사설·칼럼 **-20**(단, 구체적 기업 부담과 개선 요구가 모두 있으면 감점 없음),
      기업 영향 없는 정부 정책 소개 **-20**
  - **필수 통과 조건**: 점수만으로 무관한 기사가 통과하지 않도록, ① 규제·제도 신호·
    기업 부담·개선 요구 중 하나 이상, ② 기업·업계 주체 또는 산업 분류 중 하나 이상을
    모두 만족해야 한다. 단순 정책 발표, 기업과 무관한 정치 기사, 단순 주가·실적 기사,
    행사·인사·홍보 기사, "규제"라는 단어만 우연히 든 기사, 산업 키워드만 있고 기업
    애로가 없는 기사는 **점수와 관계없이 제외**한다. 산업 미분류 기사는 **75점 이상
    이면서** 구체적 기업 부담이나 개선 요구가 있을 때만 허용한다.
  - **연관성 점수 60점 미만 기사는 수집·요약·목록 노출에서 모두 제외한다.** 요약 API를
    부르기 전에 걸러 비용을 쓰지 않는다.
  - 한 번 수집할 때 **최대 30건**의 대표 기사만 요약·저장하고, 화면 기본 목록도
    **연관성 점수 60점 이상 중 상위 30건**만 보여준다.
  - **언론사는 제한하지 않는다.** 경제지·통신사뿐 아니라 지역지·업종 전문지도 그대로
    받고, 위 필터와 상한으로 자연스럽게 걸러지게 둔다.
  - 동일 사안의 중복 기사는 **제목 글자 2-gram 자카드 유사도 0.7 이상**이면 같은 사안으로
    본다. **지우지 않고 대표 기사에 묶어** 보관한다.
  - 이미 수집한 적 있는 기사는 다시 담지 않는다 (원문 URL 기준으로 중복 제외).
  - 수집 시점에 원문 링크로 HEAD 요청을 한 번 보내, 응답하지 않는 기사는 목록에서 뺀다.
  - 수집한 기사는 **90일이 지나면 삭제**한다. 화면 기본 표시는 최근 7일이다.
  - **일회성 백필 수집 모드**: 검색 구조를 바꾸는 등 기존 데이터를 다시 훑어야 할 때를
    대비해, 운영용과 별도로 **서버 전용 백필 모드**(상한은 운영과 같은 30건)를 둔다. 쿨다운과
    `today_new_count`를 건드리지 않으며, 공개 API나 화면 버튼으로는 실행할 수 없고
    로컬 CLI/일회성 서버 스크립트로만 실행한다. 중복 제외·중복 묶기·링크 확인·요약
    규칙은 운영 수집과 동일하게 적용하고, 실행 후 자동으로 반복하지 않는다.

#### 2) 기사 요약 카드

- 설명: 수집한 기사를 카드 형태로 보여주고, 각 기사의 핵심 애로사항을 짧게 요약해준다.
- AI가 꼭 지켜야 할 규칙:
  - 요약은 3줄 이내, 한 줄당 40자 이내로 만든다. **40자는 공백을 포함한 문자 수이며,
    한글·영문·숫자를 모두 1자로 센다.**
  - 카드에는 제목 / 언론사 / 발행일 / 요약 / 원문 링크 **5가지와, 묶인 기사가 있을 때만
    "관련 기사 N건"** 을 표시한다. 그 외의 정보는 넣지 않는다.
  - 카드 상단에 **산업별 대표 이미지**(장식용)를 둘 수 있다. `public/images/industries/`에
    미리 준비한 **로컬 이미지만** 쓰고, 기사 원문·언론사 사이트에서 이미지를 가져오지
    않는다(본문 크롤링 금지 원칙과 동일). 여러 산업에 해당하면 첫 번째 산업 이미지를,
    미분류거나 이미지 파일이 없으면 기본 이미지/CSS 대체 배경을 쓴다. 이미지 위에는
    **제목만** 올리고, 산업명·배지·관련도 점수는 표시하지 않는다.
  - **원문 링크**는 네이버 뉴스 링크(`link`)를 우선 쓰고, 없을 때만 언론사 원문
    (`originallink`)으로 연결한다. 네이버 링크가 삭제·유료화로 죽는 경우가 더 적다.
  - **"관련 기사 N건"** 을 누르면 카드 안에서 묶인 기사의 제목과 링크가 펼쳐지고, 다시
    누르면 접힌다. 화면 이동은 없다. 요약은 **대표 기사 1건에 대해서만** 만들고, 묶인
    나머지는 제목과 링크만 보관한다.
  - 목록 정렬은 **연관성 점수 내림차순**, 동점이면 발행일 최신순으로 고정한다.
    산업 필터를 걸어도 같은 정렬을 유지한다.
  - 카드의 언론사·발행일 행 오른쪽에 **연관성 점수 배지**(예: `연관성 82점`)를 작게
    표시한다. 80점 이상은 파란색, 70~79점은 옅은 파랑, 60~69점은 회색으로 구분하고,
    점수 산정 근거·키워드 목록은 표시하지 않는다.
  - 화면 상단에 **마지막 수집 성공 시각을 항상 표시**한다. 수집이 며칠째 실패해도
    사용자가 날짜만 보고 알아챌 수 있게 하기 위한 장치다.
  - 기사에 없는 내용은 절대 추측해서 쓰지 않는다.
  - **요약 실패**는 다음 세 가지를 말한다. 실패 시 임의로 문장을 만들지 말고 "요약 실패"
    문구와 원문 링크만 남긴다.
    1. API 오류 또는 타임아웃
    2. 재시도 1회 후에도 3줄·40자 규칙을 어긴 응답
    3. 빈 응답

### 예외 상황 처리

| 상황 | 화면 동작 |
|---|---|
| 수집 결과 0건 | 빈 화면 대신 "오늘 조건에 맞는 새 기사가 없습니다 (마지막 수집 08:00)" 표시. 어제 목록은 그대로 둔다. |
| 요약 API 장애 | 제목·언론사·발행일·원문 링크는 그대로 두고 요약 자리에만 "요약 실패 · 다시 시도" 버튼. 전부 실패하면 상단에 안내 띠. |
| 뉴스 API 장애 | 이전 목록을 유지하고 상단에 "오늘 수집 실패 · 마지막 수집 (날짜/시각)" 띠. 부분 성공은 받은 만큼 저장한다. **빈 화면으로 덮어쓰지 않는다.** |
| 수집이 며칠째 실패 (키 만료·한도 초과 등) | 상단의 **마지막 수집 성공 시각**이 갱신되지 않으므로, 접속한 사람이 날짜를 보고 알아챈다. 별도 알림은 보내지 않는다(비범위 유지). |
| 원문 링크 사망 | 수집 시점 HEAD 확인으로 상당수를 거른다. 열람 시점의 삭제까지는 막지 않는다. |
| 중복 오판 | 묶기만 하고 지우지 않으므로 기사가 사라지지 않는다. 유사도 임계값은 운영 2주 후 로그를 보고 조정한다. |

#### 3) 산업 필터 (고정형)

- 설명: 화면 상단 드롭다운에서 산업 하나를 고르면, 이미 수집된 기사 중 그 산업에
  해당하는 것만 걸러 보여준다. **사용자가 만들거나 관리하는 태그가 아니라, 시스템이
  정한 8개 산업 기준의 고정형 필터다.** 사용자 태그 기능(6번 비범위)과는 다른 것이다.
- 산업 목록(고정 8개): 자동차, 철강, 조선 및 해운, 에너지, 바이오, 금융, 반도체,
  정보통신. 기본값은 "전체".
- AI가 꼭 지켜야 할 규칙:
  - 산업 분류는 **별도 AI 호출 없이**, 제목+발췌문에서 고정 키워드를 찾아 판정한다
    (기사 본문 크롤링 금지 원칙 그대로 유지).
  - 한 기사가 여러 산업에 해당하면 모두 저장한다. 어디에도 해당하지 않으면 빈
    배열로 둔다 — 미분류는 오류가 아니라 정상 상태다.
  - 필터는 **화면(클라이언트) 상태로만** 처리한다. 산업을 바꿔도 페이지 이동, URL
    변경, 네이버·OpenAI 재호출, 새 서버 라우트 호출은 없다.
  - 카드 안에는 산업명·배지를 표시하지 않는다. 기존 5가지 표시 항목(제목·언론사·
    발행일·요약·원문 링크)과 관련 기사 수는 그대로 유지한다.

### nice

이번 범위에서는 두지 않는다. 위 세 기능에만 집중한다.

---

## 6. 범위와 비범위

*이번 실습에서 만들 것과 만들지 않을 것을 나눠 적는 칸입니다.*

### 만들 것

- 일일 규제 기사 자동 수집 (검색어 12개 + 관련성 점수제 포함 수집 규칙)
- 기사 요약 카드 (요약·표시 규칙 8가지 포함)
- 중복 기사 묶음 표시와 펼치기 ("관련 기사 N건")
- 마지막 수집 성공 시각 상시 표시
- 위 표의 예외 상황 6가지 처리
- **산업 필터** (고정 8개, 시스템이 키워드로 분류 — 사용자 태그 아님)

### 만들지 않을 것

- 로그인·회원가입·권한 관리 (내부 실무자용이므로 이번엔 제외)
- 이메일·슬랙 등 외부 알림 발송
- 기사 통계·차트·대시보드 (건수 추이, 키워드 분석 등, **산업별 건수 표시 포함**)
- 사용자가 직접 쓰는 메모·코멘트·태그 기능 (산업 필터는 시스템이 정한 고정 분류이지,
  사용자가 만들거나 관리하는 태그가 아니므로 이 항목과 별개다)
- 규제 개선안 자동 작성이나 정책 제안 생성
- 해외 기사 수집, 다국어 번역
- 네이티브 모바일 앱 (이번에는 Next.js 웹 하나만 만든다)
- **기사 본문 크롤링** (아래 7번 참고)

---

## 7. 보안·개인정보 검토

*사용 문서의 공개등급, 개인정보 포함 여부, API 키 관리 방법을 적는 칸입니다.*

**사용 문서의 공개등급**

- 언론사가 공개 발행한 기사만 사용한다. 대외비·내부 문서는 다루지 않는다.
- **본문을 크롤링하지 않는다.** 네이버 뉴스 검색 API가 제공하는 제목과 발췌문
  (`description`, 2~3줄)만으로 요약을 만든다. 요약 품질은 조금 낮아지지만 저작권과
  robots.txt 문제를 처음부터 피할 수 있다.
- 화면에는 제목·요약·원문 링크만 노출하고, 기사 전문은 저장하지도 보여주지도 않는다.

**접근 제어 (의도된 선택)**

- 배포된 주소는 **URL을 아는 사람이면 누구나 열 수 있다.** 로그인을 만들지 않은 것은
  누락이 아니라 결정이다. 다루는 자료가 전부 공개 보도이고, 이용자가 과 내부 5~10명으로
  한정돼 있어 로그인 관리 비용이 이득보다 크다고 판단했다.
- 다만 화면의 새로고침 버튼은 서버의 요약 API 키를 쓰므로, **쿨다운과 하루 30건 상한을
  서버에서 강제**한다. 화면 쪽에서만 막으면 우회가 가능하다.
- 향후 비공개 자료를 다루게 되면 이 결정을 다시 검토해야 한다.

**개인정보 포함 여부**

- 로그인이 없어 이용자 개인정보는 수집하지 않는다.
- 기사 발췌문에 인명·직함이 등장할 수 있으나, 이를 따로 뽑아 저장하거나 인물별로 모으지
  않는다.

**API 키 관리**

- 키는 `.env`에만 두고, `.gitignore`에 등록해 GitHub에 올라가지 않게 한다.
- 뉴스 API와 요약 API 호출은 **모두 서버 쪽 코드(API 라우트, Cron 라우트)에서만** 한다.
  키가 브라우저로 내려가면 누구나 꺼내 볼 수 있으므로, 화면 쪽 코드에서는 절대 키를
  참조하지 않는다.
- 배포 시에는 Vercel 환경변수에 등록해 사용한다.

---

## 8. 기술 스택

*개발에 사용할 도구 모음을 적는 칸입니다. 이 과정에서는 Next.js로 고정합니다.*

- **Next.js** — 화면과 서버 쪽 코드(API)를 한 프로젝트에서 처리한다.

**고정 이유**: Day 1 실습에서 쓴 것과 같은 구성이고, 3일차에 Vercel로 배포할 예정이라
그대로 이어지게 하기 위함이다.

### 함께 쓰는 외부 서비스

프레임워크는 Next.js 하나로 고정하되, 아래 네 가지는 Next.js가 대신할 수 없는 부분이라
따로 붙인다.

| 용도 | 서비스 | 선택 이유 |
|---|---|---|
| 기사 수집 | 네이버 뉴스 검색 API | 무료이고 신청 즉시 발급된다. 빅카인즈는 자료가 더 좋지만 사용 승인 대기가 있어 3일 일정에 맞지 않는다. |
| 기사 저장 | Supabase (테이블 1개) | Vercel은 서버리스라 파일로 저장할 수 없다. "이미 수집한 URL 제외" 규칙에 저장소가 필요하다. |
| 요약 생성 | OpenAI API (`gpt-5-mini`) | 원래는 Claude API로 정했으나 Anthropic 키 발급 전에 개발을 진행하기 위해, 이미 있던 OPENAI_API_KEY로 바꿨다. `gpt-5-mini`는 추론 모델이라 `reasoning_effort: 'minimal'`을 반드시 지정해야 한다 — 안 하면 토큰 예산을 내부 추론에 다 쓰고 빈 응답을 낸다. |
| 자동 실행 | Vercel Cron Jobs | `"0 23 * * *"`(UTC 23시 = KST 08시). 무료 플랜의 하루 1회 제한이 요구사항과 정확히 맞는다. |

**저장 테이블 구조** (`articles`)

`url`(고유), `title`, `press`, `published_at`, `summary`, `collected_at`,
`group_id`(중복 묶음 대표 기사 URL),
`industries`(고정 8개 산업 중 매칭된 것들의 배열, 기본값 빈 배열 — 5-1 3)번 산업 필터),
`relevance_score`(기업 규제·애로 연관성 0~100, 기본값 0 — 60점 미만은 목록에 노출하지 않음)

**저장 테이블 구조** (`collection_state`, 단일 행)

Vercel은 서버리스라 실행 간 메모리를 유지하지 못한다. 쿨다운·하루 상한·마지막 수집
성공 시각·최초 소급 여부를 판단하려면 이 값들을 어딘가에 저장해야 하는데, 그 자리가
이 테이블이다.

| 컬럼 | 용도 |
|---|---|
| `last_attempt_at` | 수집 시작 시각(성공 여부 무관). 수동 새로고침의 5분 쿨다운 판단 기준. |
| `last_success_at` | 마지막으로 (부분 성공 포함) 끝난 시각. 화면 상단 "마지막 수집 성공 시각" 표시에 쓴다. |
| `today_date` | `today_new_count`가 어느 날짜(KST) 것인지. |
| `today_new_count` | 오늘 새로 저장한 대표 기사(사안) 수. 하루 총합 30건 상한 판단에 쓴다. |
| `initial_backfill_done` | 최초 3개월 소급을 이미 했는지. |

### 운영 (과정 종료 후에도 계속 사용)

실습으로 끝내지 않고 과 업무에 계속 쓰기로 했으므로, 아래를 미리 정해 둔다.

| 항목 | 내용 |
|---|---|
| 예상 비용 | 요약 API 월 3~4천 원. Supabase(500MB)와 Vercel은 이 규모에서 무료 한도 안에 들어온다. |
| 결제·키 관리 주체 | **미정 — 운영 시작 전에 정한다.** 개인 카드로 두면 담당자가 바뀔 때 서비스가 멈춘다. |
| 장애 인지 방법 | 화면 상단의 마지막 수집 성공 시각. 매일 아침 보는 도구라 하루 이상 밀리면 눈에 띈다. |
| 정기 점검 | 주 1회 요약 검수(3번)와 함께 마지막 수집 시각을 확인한다. |

---

## 9. 개발 단위

*핵심 기능을 만들기 좋은 작은 단위로 쪼개, 만들 순서대로 정리한 칸입니다.*

1. Next.js 프로젝트를 만들고 빈 화면이 뜨는 것까지 확인한다.
2. `Article` 타입과 화면 확인용 샘플 기사 데이터를 만든다.
3. 기사 카드 컴포넌트를 만든다 (제목·언론사·발행일·요약·원문 링크 5가지).
4. 샘플 데이터로 기사 목록 화면을 만든다 (발행일 최신순, 0건일 때 안내 문구).
5. Supabase에 `articles` 테이블을 만들고 목록 화면이 여기서 읽어오게 한다.
6. 네이버 뉴스 검색 API를 호출하는 서버 라우트를 만든다 (검색어 12개, 필터 없이 원본 그대로).
7. 수집 규칙 중 키워드 필터와 기간 필터를 붙인다 (키워드 10개, 최근 7일).
8. 수집 규칙 중 연관성 점수(0~100) 계산·필수 통과 조건·60점 미만 제외와 점수순
   상위 30건 자르기를 붙인다.
9. 수집 규칙 중 이미 수집한 URL 제외를 붙인다.
10. 제목 유사도 0.7로 중복 기사를 묶고, 카드에 "관련 기사 N건"을 표시·펼치게 한다.
11. 수집 시점에 원문 링크로 HEAD 요청을 보내 죽은 링크를 걸러낸다.
12. OpenAI API로 기사 1건을 요약하는 서버 라우트를 만든다.
13. 요약 길이 규칙 검증·재시도 1회·"요약 실패" 처리를 붙인다.
14. 수집 → 요약 → 저장을 하나의 흐름으로 연결한다.
15. Vercel Cron을 붙여 매일 오전 8시(KST)에 자동 실행되게 한다.
16. 수동 새로고침 버튼을 만든다 (5분 쿨다운과 하루 총합 30건을 서버에서 강제).
17. 화면 상단에 마지막 수집 성공 시각을 항상 표시한다.
18. 예외 상황 3가지를 처리한다 (수집 0건, 뉴스 API 장애, 요약 API 장애 안내).
19. 90일이 지난 기사를 삭제하는 정리 작업을 붙인다.
19-1. 진입 인트로를 붙인다 (별도 라우트 없이 overlay, **페이지 로드마다 매번**,
    기사 데이터는 뒤에서 미리 렌더링, 다층 컬러 웨이브로 클릭 후 **2.4~3.0초**에
    걸쳐 천천히 모핑, 인트로와 메인을 350~500ms 겹쳐 잇기, 동작 줄이기에서는
    단순 fade). 메인 화면은 같은 파랑·시안·바이올렛을 낮은 투명도로 이어받고,
    미세한 gradient·border·glow까지만 쓴다.
19-2. 기사 카드를 누르면 **상세 dialog**를 연다. 표시 항목은 산업 이미지·산업명·
    제목·언론사·발행일·연관성 점수·저장된 요약·관련 기사·원문 링크뿐이며,
    **이미 목록에 로드된 데이터만 쓴다**(상세 진입 시 기사 API·OpenAI·Supabase
    재호출 없음). 기사 본문을 크롤링하거나 iframe·proxy로 복제하지 않고 원문은
    새 탭으로 연다. 별도 라우트·URL 파라미터를 만들지 않는다.
20. 디자인을 적용한다 (모바일 우선 반응형·산업별 이미지 카드 포함).
    상단 영역(페이지 배경 `#F8F9FA`, 흰색 둥근 헤더 패널, 둥근 산업 선택 영역,
    `#1A73E8` 새로고침 버튼)은 Google Workspace 계열의 담백한 톤으로 두고,
    **기사 카드 디자인은 이미 만든 것을 그대로 유지한다**(점수 배지만 추가).
21. Vercel에 배포하고 환경변수를 등록해 실제 동작을 확인한다.

**(추가) 산업 필터** — 사용자 요청으로 5-1 3)번을 위 순서에 끼워 넣지 않고 별도로
추가했다. 15~21번은 아직 미완료 상태 그대로다. 상세 내용은 PLAN.md 참고.
