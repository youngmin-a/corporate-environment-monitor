# DESIGN.md — 기업환경 모니터 설계

*[PLAN.md](PLAN.md)의 작업을 실제로 어떻게 만들지 정리한 문서다. 상세 규칙(숫자·조건)의
출처는 항상 [PRD.md](PRD.md)다.*

---

## 1. 화면 구성 (무엇이 어디에 보이는지)

화면은 하나뿐이다 (목록 페이지 1개, 별도 상세 페이지 없음). 위에서 아래로 세로 스크롤.

**모바일 (767px 이하)** — 기사 카드 1열

```
┌─────────────────────────────────┐
│  기업환경 모니터                  │  ← 제목 (네이비 배경)
│  마지막 수집: 8/3 08:00           │  ← 상단에 항상 표시
│  [ 새로고침 ]                     │  ← 쿨다운 중이면 비활성화 + 남은 시간
├─────────────────────────────────┤
│ (예외 상황일 때만) 안내 띠         │  ← "오늘 수집 실패" 등
├─────────────────────────────────┤
│ 산업 선택 [ 전체            ▾ ]   │  ← 고정 8개 산업 + 전체, 클라이언트 필터
├─────────────────────────────────┤
│ ┌───────────────────────────┐   │
│ │▒▒▒ 산업 대표 이미지 (16:9) ▒│   │  ← 장식용 로컬 이미지
│ │▒▒▒ 기사 제목 (이미지 위) ▒▒│   │  ← 그라데이션 위 흰 글씨, 최대 3줄
│ ├───────────────────────────┤   │
│ │ 언론사 · 8/3                │   │
│ │ 요약 1~3줄                  │   │  ← 카드 1개
│ │ [원문 보기]  관련 기사 2건 ▾  │   │
│ └───────────────────────────┘   │
│ ┌───────────────────────────┐   │
│ │ (카드 반복, 최신순)          │   │
│ └───────────────────────────┘   │
│  (0건일 때) "오늘 조건에 맞는     │
│   새 기사가 없습니다"             │
└─────────────────────────────────┘
```

**태블릿·데스크톱 (768px 이상)** — 기사 카드 2열, 헤더는 좌우 배치

```
┌───────────────────────────────────────────────────────┐
│ 기업환경 모니터                   마지막 수집 8/4 14:12 │
│ 기업 규제·애로사항 모니터링              [ 새로고침 ]   │
├───────────────────────────────────────────────────────┤
│ 산업 선택  [ 전체                                  ▾ ] │
├───────────────────────────────────────────────────────┤
│ ┌─────────────────────┐   ┌─────────────────────┐   │
│ │▒ 이미지 + 제목 ▒▒▒▒│   │▒ 이미지 + 제목 ▒▒▒▒│   │
│ ├─────────────────────┤   ├─────────────────────┤   │
│ │ 언론사·날짜 / 요약    │   │ 언론사·날짜 / 요약    │   │
│ │ [원문 보기] 관련 N건 ▾│   │ [원문 보기] 관련 N건 ▾│   │
│ └─────────────────────┘   └─────────────────────┘   │
│           (2열 반복, 최신순)                          │
└───────────────────────────────────────────────────────┘
```

- **카드 안에 들어가는 것은 딱 5가지 + 관련 기사 수**: 제목 / 언론사 / 발행일 / 요약 /
  원문 링크. "관련 기사 N건"을 누르면 카드 안에서 그 아래로 목록이 펼쳐진다(페이지 이동
  없음). **산업명·배지는 카드에 넣지 않는다.** 상단 이미지는 장식일 뿐이라
  `alt=""`로 두고, 이미지 위에는 **제목만** 올린다(언론사·요약·링크는 흰 본문 영역).
- **산업 선택**은 `label` + 기본 HTML `select` 하나뿐이다. "전체"가 기본값이고, 고르면
  이미 화면에 내려받은 기사 목록을 클라이언트에서 걸러 보여줄 뿐이다 — 서버 재호출도
  URL 변경도 없다 (아래 2-4 참고).
- **반응형**: 모바일 우선. 전체 콘텐츠 최대 너비 1180px, 좌우 여백은 모바일 16px /
  태블릿 24px / 데스크톱 32px. 헤더와 산업 필터는 전체 너비를 쓰고, **기사 목록만**
  `grid-cols-1 md:grid-cols-2`로 나뉜다. 1180px보다 넓어져도 콘텐츠 폭은 그대로다.
- 색: 헤더 네이비(`#1E3A5F`), 버튼·강조 파랑(`#2563EB`), 배경 흰색/연회색.

### 1-1. 산업 이미지 선택과 대체(fallback) 흐름

```
기사의 industries 배열   →   getArticleImage()          →   <Image src=... />
(수집 시 분류된 값)           첫 번째 산업의 이미지 경로        16:9 고정, object-cover
                              ↓ 비었거나 매핑에 없으면
                              기본 이미지 경로
                                       ↓ 파일이 없어 로딩 실패하면 (onError)
                              CSS 그라데이션(네이비→파랑)으로 대체
                              — 깨진 이미지 아이콘을 보여주지 않는다
```

경로 매핑(`INDUSTRY_IMAGES`)과 선택 함수(`getArticleImage`)는 산업 상수와 함께
`lib/industries.ts`에 둔다. 이미지 경로는 `Article` 타입에도 Supabase에도 저장하지
않는다 — 산업값만 있으면 화면에서 매번 계산할 수 있기 때문이다.

### 1-2. 연관성 점수 계산 위치와 데이터 흐름

점수 규칙과 상한 상수는 **`lib/relevance.ts` 한 곳**에 모은다. 화면 컴포넌트나
파이프라인 안에 키워드·숫자를 흩어놓지 않는다.

```
[신규 수집]  제목 + 발췌문(description) + industries
                → calculateRelevanceScore()  → 0~100
                → passesRelevanceGate()      → 필수 조건·제외 유형 판정
                → 60점 미만이면 요약·저장 대상에서 제외

[기존 기사]  제목 + summary (description 컬럼이 없어 요약으로 대신한다)
                → 같은 함수 → articles.relevance_score UPDATE
                → 점수가 낮아도 행을 지우지 않고 화면에서만 숨긴다

[화면 조회]  relevance_score >= 60
                → 점수 내림차순, 동점이면 published_at 최신순
                → 상위 30건만 내려보낸다
```

이미지 경로와 달리 점수는 **DB 컬럼(`relevance_score`)에 저장한다.** 조회 단계에서
정렬·상한을 걸어야 하는데, 매번 화면에서 계산하면 그럴 수 없기 때문이다.

### 1-2-1. 진입 인트로 (로드마다 매번)

```
[페이지 로드]  overlay 표시 (visible)
                 본 적 있음을 저장하지 않으므로 서버·클라이언트가 같은 화면을
                 그린다. 뒤에는 메인 화면이 이미 렌더링돼 있다
             [모니터링 시작] 클릭 (visible → exiting)
                 → 버튼 압축 → 유체 확장 → 컬러 웨이브 → 콘텐츠 용해 → 메인 공개
                 → 2,700ms 뒤 종료 (exiting → hidden, DOM에서 제거)
```

별도 `/intro` 라우트를 만들지 않고 URL도 바꾸지 않는다. 인트로는 데이터를 기다리는
로딩 화면이 아니라 이미 준비된 화면을 여는 연출이므로, 버튼을 눌러도 API를 새로
부르지 않는다. `prefers-reduced-motion`에서는 확대·이동을 빼고 180ms fade만 쓴다.

**컬러 웨이브 구성** — 인트로 배경에는 파랑→시안 / 바이올렛→인디고 / 그린→파랑
웨이브 3층이 8~14초 주기로 천천히 출렁인다. 버튼을 누르면 아래 순서로 화면을 휩쓴다
(하위 단계는 별도 타이머 없이 CSS `animation-delay`로 순서를 잡는다).

```
phase        시작    길이    내용
─────────────────────────────────────────────────────────────
charging       0ms   340ms  버튼 압축(0.94) + 빛이 중앙으로 응축
expanding    340ms   640ms  버튼 뒤 유체 blob이 천천히 확장
flowing      760ms   940ms  웨이브 3층이 굽이치며 교차 (파랑·시안 /
                            바이올렛·인디고 / 그린·시안)
dissolving  1,260ms  500ms  제목·설명·버튼 용해 (opacity·scale·blur 2px)
revealing   1,900ms  740ms  메인 공개, 인트로와 약 420ms 겹침
─────────────────────────────────────────────────────────────
총 2,700ms  (easing: cubic-bezier(0.22, 1, 0.36, 1))
```

인트로 overlay를 먼저 지우고 메인을 띄우지 않는다. `revealing`이 시작되는
1,900ms부터 메인이 떠오르는 동안 인트로가 남아 있어 두 화면이 겹쳐 이어진다.

메인 화면은 같은 파랑·시안·바이올렛을 **훨씬 낮은 투명도**로 이어받아(`.app-background`)
인트로에서 넘어올 때 색이 끊기지 않게 한다. 메인 배경 자체는 움직이지 않는다.

### 1-2-2. 기사 상세 dialog

카드를 누르면 별도 페이지로 이동하지 않고 카드 자리에서 상세 패널이 확장된다.

```
[카드 클릭]  클릭 좌표 ripple 1회
               → 카드 미세 출렁임 (scale ±0.008, rotate ±0.2도)
               → 카드 위치·크기에서 패널로 확장 (CSS 변수로 delta 전달)
               → overlay + 내부 정보 순차 표시
             총 1.0~1.4초

[표시 항목]  산업 이미지 / 산업명 / 제목 / 언론사 / 발행일 / 연관성 점수
             / 저장된 요약 / 관련 기사 / 원문 링크
             — 모두 목록에 이미 로드된 데이터다
```

**상세를 열 때 네트워크 요청이 없다.** 기사 API·OpenAI·Supabase를 다시 부르지
않고, 원문은 `원문 기사 열기` 링크로 새 탭에서만 연다(iframe·proxy·크롤링 금지).
관련 기사로 넘어갈 때도 패널을 닫았다 열지 않고 내부 콘텐츠만 교체한다.

데스크톱은 중앙 modal(최대 820px), 모바일은 하단 시트(최대 92dvh)로 같은
컴포넌트를 쓴다. 접근성은 native `<dialog>`의 `showModal()`에 기대어 focus trap과
Escape를 얻고, 닫을 때 원래 카드로 focus를 되돌린다.

### 1-3. 점수 배지와 상단 영역 디자인

- 점수 배지는 카드의 **언론사·발행일 행 오른쪽**에만 넣는다. 80점 이상은 파란색,
  70~79점은 옅은 파랑, 60~69점은 회색이며, 스크린리더에는
  `기업 규제 및 애로 연관성 82점`으로 읽힌다.
- **기사 카드의 나머지 디자인은 바꾸지 않는다** — 산업 이미지, 제목 오버레이, 본문
  배치, 1·2열 grid, hover 효과, 관련 기사 펼치기 애니메이션 모두 그대로 둔다.
- 상단 영역만 Google Workspace 계열로 정리한다: 페이지 배경 `#F8F9FA`(아주 옅은
  radial gradient 허용), 헤더는 흰색 둥근 패널(`border: 1px solid #E8EAED`, 낮은
  그림자), 산업 선택은 높이 52~56px의 둥근 검색창형, 새로고침 버튼은 `#1A73E8`.

---

## 2. 데이터 흐름 (입력 → 처리 → 출력)

세 가지 흐름이 있다. **셋 다 같은 "수집→필터→요약→저장" 파이프라인 함수 하나를
공유**하고, 그 함수를 누가/언제 부르느냐만 다르다.

### 2-1. 자동 수집 (매일 오전 8시)

```
입력                     처리                              출력
────────────────────────────────────────────────────────────────
Vercel Cron 신호   →   collection_state 확인:            →   Supabase
(매일 08:00 KST)       초회면 3개월 소급 모드,               articles에 새 기사 저장
                        아니면 최근 7일 모드                  collection_state 갱신
                        (오늘 여유분 = 40 - today_new_count)  (last_success_at,
                        여유분 0이면 API 호출 없이 종료         today_new_count,
                        ↓                                    initial_backfill_done)
                        네이버 뉴스 검색 API 호출
                        (검색어 12개: 직접 규제 4 +
                         산업 8, "기업 규제" 등)
                        ↓
                        기간 필터
                        ↓
                        이미 수집한 URL 제외
                        ↓
                        제목·발췌문 정규화 → 산업 분류
                        ↓
                        연관성 점수 계산 (0~100)
                        ↓
                        필수 통과 조건 + 60점 미만 제외
                        ↓
                        원문 링크 살아있는지 확인(HEAD)
                        ↓
                        제목 유사도 0.7로 중복 묶기
                        (대표는 점수 높은 기사, 동점이면 최신)
                        ↓
                        연관성 점수순 상위 30건 선정
                        ↓
                        선정된 대표 기사만 OpenAI로 요약
                        (industries·relevance_score는 앞 단계에서
                         계산해 둔 값을 그대로 저장에 재사용)
```

> **60점 미만을 요약 앞에서 자르는 이유**: 요약은 유일한 유료 API 호출이다. 관련성이
> 낮은 기사에 비용을 쓰지 않도록, 점수 필터를 반드시 요약보다 앞에 둔다.

> PRD 4번 "필터링(기간·관련성 점수·중복·링크 확인) → 산업별 우선 목표 + 관련도순
> 40건 자르기" 순서를 그대로 따른다. 중복을 먼저 묶어야 "40건"이 실제 사안 40개를
> 뜻하게 된다 — 나중에 묶으면 중복 기사가 자리를 차지해 사안 수가 40건보다 줄어든다.
>
> ⚠️ **주의**: HEAD 확인을 40건 컷 이전에 하므로, 필터 통과 기사가 많은 날은 HEAD
> 요청이 수십~수백 건 나갈 수 있다. Vercel 함수 실행 시간 안에 끝나는지 구현 시
> 확인이 필요하다 (design-validator 리스크 항목 참고).
>
> **일회성 백필 수집 모드**: 운영 흐름과 같은 파이프라인 함수(`runCollection`)를
> `mode: 'backfill'`로 부르면 쿨다운·`today_new_count`를 건드리지 않고 상한만
> 80건으로 늘려 실행한다. Next.js 라우트가 아니라 `npm run backfill:collect`
> (`scripts/backfill-collect.ts`)로만 실행할 수 있다.

### 2-1-1. 상태 저장 (`collection_state`)

Vercel은 서버리스라 실행이 끝나면 메모리에 아무것도 안 남는다. 그래서 "쿨다운 5분",
"오늘 몇 건 찼는지", "마지막으로 성공한 게 언제인지", "3개월 소급을 이미 했는지"를
Supabase에 **단일 행짜리 테이블**로 저장해 둔다.

| 컬럼 | 용도 |
|---|---|
| `last_attempt_at` | 수집이 **시작될 때**(성공 여부와 무관) 갱신. 쿨다운 판단 기준. |
| `last_success_at` | 수집이 (부분 성공이라도) 끝나면 갱신. 화면 상단 표시용. |
| `today_date` | `today_new_count`가 몇 월 며칠(KST) 것인지. 날짜가 바뀌면 실행 시작 시 카운트를 0으로 리셋한다. |
| `today_new_count` | 오늘 새로 저장한 대표 기사(사안) 수. |
| `initial_backfill_done` | 3개월 소급을 이미 했는지. 처음엔 `false`, 한 번 끝나면 `true`. |

수집 파이프라인은 시작할 때 이 행을 읽어 "쿨다운 통과했나 / 오늘 여유분 얼마나 남았나 /
초회 모드인가"를 정하고, 끝날 때 이 행을 갱신한다. 자세한 컬럼 정의는
[PRD.md](PRD.md) 8번을 따른다.

### 2-2. 사용자가 접속했을 때

```
입력                入력   처리                    출력
────────────────────────────────────────────────────────
사용자가 사이트  →   Supabase에서 최근 7일   →   카드 목록 화면
접속                  기사를 최신순으로 조회        (서버에서 미리
                                                    그려서 보냄)
```

### 2-3. 수동 새로고침

```
입력              처리                                    출력
──────────────────────────────────────────────────────────────
[새로고침]   →   collection_state 조회:                →   (통과 시) 2-1과
버튼 클릭         now - last_attempt_at >= 5분?            동일한 파이프라인
                  20 - today_new_count > 0?                실행 후 화면 갱신
                  ↓ 둘 중 하나라도 아니면                   (막히면) "N분 후 다시
                  거부하고 이유를 알려줌                     시도" 안내 문구만
```

**중요한 설계 원칙**: 새로고침 버튼은 화면(브라우저)에서 곧바로 네이버·OpenAI API를
부르지 않는다. 반드시 **우리 서버를 한 번 거쳐서** 부른다. 그래야 API 키가 브라우저에
노출되지 않고, 쿨다운·상한 규칙도 우회할 수 없다.

### 2-4. 산업 필터 (클라이언트 전용, 새 서버 호출 없음)

```
입력              처리                                    출력
──────────────────────────────────────────────────────────────
드롭다운에서   →   이미 내려받은 groups 배열을           →   필터링된 카드
산업 선택          group.representative.industries에         목록으로 다시
                   선택값이 포함되는지로 filter()             그리기(같은 화면)
```

분류(`classifyIndustries`)는 수집 시점(2-1)에 이미 끝나 있다. 화면에서는 그 결과를
걸러 보여주기만 하므로 네이버·OpenAI를 다시 부르지 않고, 새 라우트도 필요 없다.
"전체"를 고르면 산업 미분류(빈 배열) 기사를 포함한 전체를 그대로 보여준다.

---

## 3. 기술 선택

**기본은 Next.js.** PRD 8번에 고정된 대로, 화면과 서버 코드(API)를 한 프로젝트 안에서
같이 만든다. 오늘 안에 Vercel로 배포하는 걸 기준으로, 그 위에 아래 것들을 추가로 쓴다.
전부 무료로 시작할 수 있는 것들만 골랐다.

| 기술 | 왜 필요한가 (쉬운 설명) |
|---|---|
| **Next.js** | 화면(사람이 보는 부분)과 서버(키를 안전하게 다루는 뒷단)를 한 프로젝트로 같이 만드는 도구. 오늘 Vercel에 그대로 올릴 수 있다. |
| **TypeScript** | Next.js에 기본 포함. 코드에 오타나 실수가 있으면 실행하기 전에 미리 알려주는 맞춤법 검사기 같은 것. |
| **Tailwind CSS** | 미리 정한 네이비·파랑 색과 카드 모양을 빠르고 일관되게 입히는 도구. 색상을 여기저기 흩어놓지 않고 한 곳에서 관리할 수 있다. |
| **Supabase** | 수집한 기사를 저장해 두는 온라인 데이터베이스 서비스. 우리가 직접 서버 컴퓨터를 관리할 필요 없이, 이미 발급받은 계정으로 바로 쓸 수 있다. |
| **Supabase 연결 라이브러리** (`@supabase/supabase-js`) | Next.js 코드가 Supabase 데이터베이스에 "기사 읽어줘 / 저장해줘"라고 말을 거는 통역 도구. |
| **네이버 뉴스 검색 API** | 이미 있는 네이버의 뉴스 검색 기능을 우리 서버에서 그대로 불러 쓰는 창구. 별도 설치 없이 인터넷 요청만으로 쓴다. |
| **OpenAI API 연결 라이브러리** (`openai`) | 기사를 AI에게 보내 "3줄로 요약해줘"라고 시키고 답을 받아오는 통역 도구. 원래 Claude API로 정했으나 Anthropic 키가 없어 이미 있던 OpenAI 키로 바꿨다. |
| **Vercel Cron** | 매일 정해진 시각(오전 8시)에 "지금 수집 시작해"라고 자동으로 눌러주는 알람. 새로 설치하는 프로그램이 아니라 Vercel 설정 파일 한 줄로 켜는 기능이다. |

**오늘 배포 가능한 이유**: 위 목록 전부 Vercel과 잘 맞물리는 조합이다(Next.js는
Vercel이 만든 도구라 별도 설정이 거의 없고, Supabase·네이버·OpenAI는 모두 "인터넷
주소로 요청 보내는" 방식이라 서버를 따로 설치할 필요가 없다). 필요한 API 키를 Vercel
환경변수에 등록하기만 하면 된다.

### 폴더 구조 (Next.js App Router 기준)

```
my-app/
  app/
    page.tsx              ← 기사 목록 화면 (2-1)
    api/
      collect/route.ts    ← 수집 파이프라인 (Cron과 새로고침이 함께 호출)
      cron/route.ts       ← Vercel Cron이 매일 8시에 부르는 문
  lib/
    naver.ts               ← 네이버 API 호출
    collector.ts            ← 수집 규칙(필터·중복·정렬)
    summarize.ts             ← OpenAI 요약
    supabase.ts               ← Supabase 연결
    collectionState.ts        ← collection_state 읽기/쓰기 (쿨다운·일일상한·초회여부)
    industries.ts              ← 산업 타입·선택지·키워드·classifyIndustries() (2-4)
    classification.ts          ← 이슈·근거·긴급도·기관 분류 (AI 없이 키워드 판정)
    publishers.ts               ← 도메인 → 언론사명 매핑
    enrich.ts                    ← 조회 결과에 파생 정보 붙이기 (1회 계산)
    clustering.ts                 ← 이슈 군집화 (보수적 판정)
    dashboard.ts                   ← 검색·필터·정렬·지표·인사이트 계산
    personalState.ts               ← 읽음·검토·저장·메모·보고서 (localStorage)
    report.ts                       ← 브리핑 Markdown·CSV 생성 (AI 없음)
  components/
    Dashboard.tsx             ← 대시보드 오케스트레이터 (클라이언트)
    CommandCenter.tsx          ← 상단 상태 패널
    MetricCards.tsx / InsightStrip.tsx / FilterToolbar.tsx / FilterDrawer.tsx
    InsightPanel.tsx / ReportPanel.tsx
    ArticleCard.tsx / ArticleDetailDialog.tsx / IntroOverlay.tsx
  scripts/
    quality-audit.ts          ← read-only 품질 자기검증
  .github/workflows/
    nightly-quality-audit.yml ← 야간 감사 (03:00 KST)
```

> Cron 스케줄용 `vercel.json`은 **아직 저장소에 없다.** 현재 자동 수집은 예약돼
> 있지 않고 화면의 새로고침 버튼으로만 돈다 (PLAN 미구현 항목 참고).

---

## 참고: bkit 표준 설계 문서와의 차이

bkit의 `/pdca design`은 원래 3가지 아키텍처 안(최소 변경 / 클린 아키텍처 / 실용적
균형)을 비교해 사용자가 고르게 하고, `docs/02-design/features/`에 정식 템플릿으로
저장한다. 이번엔 요청하신 형식(화면 구성·데이터 흐름·기술 선택 3가지)을 그대로 따라
가벼운 문서로 프로젝트 루트에 만들었다. 정식 템플릿(아키텍처 옵션 비교 등)이 필요하시면
말씀해주세요.

---

## 5. 대시보드 화면 구조 (2026-08-05 확장)

### 5-1. 3단 레이아웃

```
┌──────────────────────────────────────────────────────────┐
│ Command Center  상태 라인 · 제목 · 신선도 · 수집 통계 · 새로고침 │
├──────────────────────────────────────────────────────────┤
│ 핵심 지표 카드 8개 (클릭 = 필터 적용, 숫자 count-up)            │
├──────────────────────────────────────────────────────────┤
│ 이슈 분석 스트립 (가로 스크롤, 자동 회전 없음)                   │
├──────────────────────────────────────────────────────────┤
│ 검색 · 산업 select · 정렬 · 상세 필터 · 보기 방식 · 적용 chip     │
├───────────────────────────────────┬──────────────────────┤
│ 기사 피드 (카드 / 압축 / 이슈 군집)      │ 인사이트 패널 (sticky)  │
│ 1열(모바일) · 2열(768px~)             │ 산업·추세·언급 주체     │
└───────────────────────────────────┴──────────────────────┘
                    선택 시 하단 고정 액션 바
```

- 1280px 미만에서는 인사이트 패널이 피드 아래로 내려온다(숨기지 않는다).
- 상세 필터·브리핑은 640px 이상에서 오른쪽 drawer, 그 아래에서는 bottom sheet다.
- 기사 상세는 데스크톱 중앙 modal(최대 900px·90dvh), 모바일 full-height sheet(94dvh).

### 5-2. 색 사용 규칙

기존 파랑 `#1A73E8`·인디고 `#4F46E5`가 기본, 시안 `#22D3EE`·바이올렛 `#7C3AED`가
보조다. 지표 카드마다 다른 색을 주지 않고 **역할별로만** 쓴다.

| 색 | 쓰는 곳 |
|---|---|
| 파랑·인디고 | 지표·선택 상태·기본 강조 |
| 시안 | 진행·흐름(상태 라인, ripple, 컬러 라인) |
| 바이올렛 | 이슈 유형 배지, 인사이트 막대 끝 |
| 그린 | 기업 직접 발언, 보고서 반영 상태 |
| 주황·코랄 | 지연·오류 상태 포인트 (면적을 넓히지 않는다) |

### 5-3. 상세 요약 레이아웃

`상세 요약` 제목 아래에 문장을 한 줄씩 놓고 왼쪽에 `01`~`08` 번호를 붙인다. 번호는
중요도 순위가 아니다. 문장마다 45ms씩 늦게 올라오고(전체 350ms 이내), 각 문장은
opacity 0→1 · translateY 6px→0으로 260ms 동안 나타난다. 타이핑·글자 분해 효과는
쓰지 않는다. reduced-motion에서는 stagger 없이 한 번에 fade한다.

### 5-4. 자동 품질 보고서

`reports/nightly/YYYY-MM-DD.md`에 실행 정보·검사 결과 표·발견된 문제(심각도순)·
전일 대비 변화를 남기고, `reports/quality-baseline.json`에 기준선을 저장한다.
이 정보는 **일반 사용자 화면에 노출하지 않는다** — CI artifact와 저장소 파일로만 본다.

---

## 6. 기업환경 AI 분석관 (2026-08-05)

### 6-1. 화면 진입점

우측 하단 고정 버튼(`components/AgentEntryButton.tsx`)으로 연다. 별도 route가
아니라 `components/AgentPanel.tsx`가 `<dialog>`를 열어 데스크톱은 오른쪽 drawer
(440px), 모바일은 하단 시트로 보여준다 — 기존 `FilterDrawer`/`ReportPanel`의
`.filter-dialog`/`.filter-drawer` 클래스 계열을 그대로 확장했다.

다중 선택 시 뜨는 하단 action-bar와 겹치지 않도록, 그 bar가 보이는 동안
`AgentEntryButton`은 위로 올라간다(`is-lifted`).

### 6-2. 서버 파이프라인

```
사용자 메시지
  → 슬래시 명령어 파싱 (lib/agentCommands.ts)
  → 질문 분석 LLM (lib/agentQuery.ts, 실패해도 원문 질문으로 계속 진행)
  → 하이브리드 검색 (lib/agentSearch.ts: 키워드+동의어+메타데이터+점수+최신성)
  → 근거 0건이면 답변 생성 없이 종료
  → 근거 있으면 스트리밍 답변 생성 (lib/agentPrompt.ts 시스템 프롬프트)
  → 실제 인용된 출처만 추출, 후속 질문 생성 (lib/suggestedPrompts.ts)
```

스트리밍은 `data: <JSON>\n\n` 자체 프레이밍이다(`app/api/agent/chat/route.ts`,
`lib/agentClient.ts`) — 새 스트리밍 라이브러리를 추가하지 않았다.

### 6-3. 서버/클라이언트 경계

Supabase·OpenAI를 부르는 `lib/agentSearch.ts`·`lib/agentQuery.ts`·`lib/agentPrompt.ts`·
`lib/articles.ts`는 **클라이언트 컴포넌트에서 런타임 import하지 않는다.** 클라이언트는
`lib/agentClient.ts`(fetch 기반)·`lib/agentCommands.ts`·`lib/agentPresetChips.ts`·
`types/agent.ts`(타입 전용)만 쓴다.

### 6-4. 상세 dialog 연결

인용 번호나 출처 카드를 누르면 기존 `ArticleDetailDialog`를 그대로 연다(별도
구현 없음). 상세 화면에 필요한 전체 데이터(`EnrichedGroup`)는 채팅 응답의
`sourceGroups`에 이미 포함돼 있어 추가 요청이 없다. native `<dialog>`는
`showModal()`끼리 중첩돼도(분석 패널 위에 상세 dialog) 각자 top layer에서
독립적으로 focus trap·Escape를 처리한다 — 별도 inert 관리를 추가하지 않았다.

닫기(버튼·backdrop·Escape)는 dialog가 실제로 닫힌 뒤에만 진입 버튼으로 focus를
되돌린다 — 열려 있는 동안 바깥 요소로 focus를 옮기면 브라우저가 막기 때문에,
`dialog.close()` 호출 뒤 다음 tick(setTimeout 0)에서 focus를 옮긴다.

### 6-5. reduced-motion

패널 진입/메시지 등장/출처 카드/추천 질문 stagger는 `prefers-reduced-motion`에서
모두 짧은 opacity 전환으로 대체한다. 기능(전송·스트리밍·인용 클릭·필터 chip)은
그대로 동작한다.

## 7. 브랜딩·체크박스·전체 기사 탐색 (2026-08-05)

### 7-1. 로고

`components/CommandCenter.tsx`가 `public/재정경제부.svg`를 일반 `<img>`로 연다
(Next `<Image>`는 SVG 최적화에 `dangerouslyAllowSVG` 설정이 따로 필요해, 정적
공개 자산에는 맞지 않는다고 판단했다). `onError`로 실패를 감지해 실패 시
아무것도 그리지 않는다(깨진 이미지 아이콘 금지). 크기는 `.header-agency-logo`
(globals.css)에서 `max-width`만 반응형으로 제한하고 `height`는 Tailwind
유틸리티로 조절해 원본 비율(약 3.16:1)을 그대로 지킨다.

### 7-2. 체크박스

`app/globals.css`의 `.select-checkbox` 클래스가 `appearance: none` 위에 테두리·
그림자·`:checked`의 그라디언트 배경과 `::after` 체크 마크를 직접 그린다. 카드
보기는 기존 `.card-select`(이미지 위 반투명 wrapper, 40×40으로 확대)를, 압축
목록은 새 `.compact-row__check-wrap`(40×40, 음수 margin으로 텍스트 정렬은
유지)을 클릭 영역으로 쓴다. 두 곳 모두 `<label onClick={stopCardOpen}>`으로
감싸 체크박스 클릭이 카드 열기로 번지지 않게 하면서 label-input 연결과
접근 가능한 이름(`aria-label`)을 함께 준다.

### 7-3. 전체 기사 화면

```
app/articles/page.tsx        (서버, force-dynamic) — searchParams 파싱 + 총 건수 조회
  └─ ArchiveControls          (클라이언트) — 산업/정렬 select, AI 분석관 진입점
  └─ <Suspense fallback=ArchiveListSkeleton>
       └─ ArchiveListSection  (서버) — getArticleGroupsPage() 호출
            └─ ArticlesArchive (클라이언트) — 카드 grid·pagination·상세 dialog
app/articles/error.tsx        — 조회 실패 시 안내 + 재시도(reset) + 대시보드 복귀
```

`lib/articles.ts`의 `getArticleGroupsPage({ page, industry, sort })`가 Supabase
`range()` + `count: 'exact'`로 페이지 단위(기본 30건, `MAX_VISIBLE_ARTICLES`
재사용)만 가져온다 — 152건을 한 번에 클라이언트로 내려보내지 않는다. `page`·
`industry`·`sort`는 URL 쿼리에 실려 있어 브라우저 뒤로가기·새로고침이 그대로
동작한다. 페이지 전환은 Next.js의 `<Link>` 네비게이션 + 서버 컴포넌트 스트리밍을
그대로 쓰므로, 헤더(`ArchiveControls`)는 다시 그려지지 않고 목록 영역
(`Suspense` 경계)만 로딩 스켈레톤으로 바뀐다 — 별도의 클라이언트 fetch 취소
로직 없이 Next가 이전 요청을 대체한다.

카드·상세 dialog·개인 상태(읽음·저장·브리핑)는 대시보드와 동일한
`ArticleCard`/`ArticleDetailDialog`/`lib/personalState.ts`를 그대로 재사용한다
— 로직을 복제하지 않았다.

### 7-4. AI 분석관과의 관계

`lib/agentSearch.ts`는 이번 작업 이전부터 `getAllArticleGroupsForSearch()`로
DB 전체(관련성 60점 이상, 최대 1000건)를 60초 캐시로 조회해 검색해 왔다 —
화면에 내려온 배열을 필터링하는 구조가 아니었다. 이번에는 `/articles`
화면에도 `AgentEntryButton`/`AgentPanel`을 그대로 얹어 진입 경로만 넓혔고,
검색 로직은 손대지 않았다. `ArchiveControls`가 선택된 산업을
`currentIndustryScope`로 넘겨 분석관의 기본 검색 범위에 반영한다.

## 8. 기사 선택 체크박스 재디자인 (2026-08-05)

### 8-1. 크고 어두운 외부 패널 제거

이전에는 `.card-select`가 `background: rgba(15, 23, 42, 0.45)` + `backdrop-filter`로
체크박스보다 큰 어두운 사각형 받침을 그렸다. 이 받침이 기사 이미지 위에서 별도의
버튼 덩어리처럼 보였기 때문에 **배경을 완전히 걷어내고 투명한 클릭 영역만** 남겼다.
대비는 받침이 아니라 체크박스 자신의 테두리·그림자·backdrop blur가 맡는다.

### 8-2. floating control 디자인

`.select-checkbox`는 native `<input type="checkbox">`에 `appearance: none`을 주고
직접 그린다. 두꺼운 외곽 프레임 대신 얇은 테두리와 빛·투명도 차이로 입체감을 낸다.

- **미선택**: 배경 `rgba(255,255,255,0.9)` + `backdrop-filter: blur(6px)`,
  테두리 `1.75px solid rgba(148,163,184,0.85)`, 반경 7px. 그림자는 ① 밝은 사진에서
  경계가 사라지지 않도록 두르는 1px 헤어라인 `0 0 0 1px rgba(15,23,42,0.18)`,
  ② 떠 있는 느낌의 `0 2px 8px rgba(15,23,42,0.22)`, ③ 내부 상단 하이라이트,
  ④ 시안 계열 내부 링 `inset 0 0 0 1px rgba(34,211,238,0.16)` 네 겹이다.
  테두리를 1.5px가 아니라 1.75px로 둔 이유는 dpr 1.25 화면에서 1.5px가 1 device px로
  스냅돼 지나치게 얇아지기 때문이다(1.75px는 2 device px로 스냅된다).
- **hover**: 테두리를 시안 `rgba(34,211,238,0.9)`으로, 배경을 더 밝게, 그림자를
  시안 계열로 바꾸고 `translateY(-1px) scale(1.03)`만 준다. glow·pulse는 쓰지 않는다.
- **checked**: `linear-gradient(135deg, #22D3EE 0%, #1A73E8 46%, #4F46E5 100%)`에
  테두리를 투명으로 두고 `0 4px 12px rgba(26,115,232,0.28)` + 내부 흰 링을 얹는다.
- **focus-visible**: `outline: 3px solid rgba(34,211,238,0.85)`, `outline-offset: 3px`.
  hover 스타일과 겹쳐도 outline이 항상 보인다.
- **disabled**: 중립 회색 배경·테두리에 체크 표시만 `#94a3b8`로 낮춘다. opacity로만
  구분하지 않으므로 disabled 상태에서도 체크 여부를 읽을 수 있다.

### 8-3. 체크 표시

`::after`로 만든 CSS 전용 체크다(아이콘 라이브러리를 새로 넣지 않았다).
6×11px 박스의 오른쪽·아래 테두리만 2.25px로 남기고 45도 회전시켜 왼쪽 아래에서
오른쪽 위로 올라가는 형태를 만든다. 흰색이며 `border-radius: 1px`로 끝을 살짝
둥글렸다. 체크 표시는 **항상 DOM에 두고** opacity·scale로만 여닫는다 —
선택은 200ms(40ms 지연)로 조금 느긋하게, 해제는 150ms로 더 빠르게 사라져
해제가 선택의 역재생처럼 늘어지지 않는다.

### 8-4. 크기와 클릭 영역

| | 보이는 체크박스 | 클릭 영역 | 카드 모서리에서 |
| --- | --- | --- | --- |
| 데스크톱 | 22px | 40×40px | 13px |
| 모바일·터치 | 23px | 44×44px | 약 13px |

클릭 영역(`.card-select`)은 배경이 없는 투명 영역이라 크기를 키워도 시각적으로
드러나지 않는다. 압축 목록은 같은 역할을 `.compact-row__check-wrap`이 맡되,
흰 배경 위라 hover에서 옅은 시안 tint만 준다.

### 8-5. 밝고 어두운 이미지에서의 대비

9개 산업 이미지 전부에 미선택·선택 체크박스를 얹어 확인했다. 흰 클린룸
(semiconductor)·흰 실험실(bio)처럼 가장 밝은 사진에서는 1px 헤어라인이 경계를
만들고, 어두운 제철소(steel)·야간 항만(shipbuilding)에서는 밝은 반투명 표면이
떠 보인다. 선택 상태의 파랑 gradient는 모든 이미지에서 즉시 구분된다.

### 8-6. reduced-motion

`prefers-reduced-motion: reduce`에서 scale pop 애니메이션과 hover의 이동·확대를
없애고(`animation: none`, `transform: none`), 테두리·배경은 0.1s linear로,
체크 표시는 opacity 0.1s로만 바꾼다. 체크 표시의 45도 회전 형태는 그대로 유지해
모양이 달라지지 않는다.
